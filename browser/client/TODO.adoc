* remove `XMLHttpRequest` for `window.fetch` code style

== Editor

* Autocomplete must be contextualized (using span class);
* Create contextualized context menu (using span class);
* Add autocomplete and menu to testEditor Builder.



== Rendering

* Split into blocks rendered separately
* Some blocks would be rendered locally (full text)
* Some blocks should be rendered into the server
* By default, everything is rendered into the server when previewing
* Only editor preview allows local rendering

== Renderer Extension Revival

* It should be possible to extends blocks renderer, it should be simple to add the following renderers:
** CAD DSL
** TQL

